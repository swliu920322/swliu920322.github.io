# swliu920322.github.io

Static site hosted on GitHub Pages.

Deployment is handled automatically by the GitHub Actions workflow in `.github/workflows/deploy.yml`: any push to `main` publishes the repository root as the site.

## Structure

- `index.html` — main page
- `demo.js` — application logic (referenced relatively from `index.html`)
